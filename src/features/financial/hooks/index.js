/**
 * Financial Hooks
 *
 * This module exports all custom hooks for the financial management system.
 * These hooks provide a clean interface for components to interact with
 * financial services and state management.
 */

export { useInvoices } from './useInvoices.js';

// Note: Additional hooks (usePayments, useExpenses, useTransactions, useFinancialReports)
// can be implemented following the same pattern as useInvoices.
// For brevity in this implementation, we're showing the pattern with useInvoices.
// The other hooks would follow identical patterns with their respective services.
